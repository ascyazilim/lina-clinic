spring:
  datasource:
    url: jdbc:postgresql://127.0.0.1:5433/lina_clinic_db
    username: lina_user
    password: lina_password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: 0
